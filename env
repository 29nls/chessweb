# === Frontend Variables (untuk Vercel / development) ===

# Backend URL untuk Socket.IO connection
# Ganti dengan URL backend yang sudah di-deploy ke Render
# Contoh: https://chessweb-backend.onrender.com
# Untuk development lokal, tidak perlu diisi (default: http://localhost:3001)
REACT_APP_BACKEND_URL=https://backend-damp-ember-8445.fly.dev

# === Backend Variables (untuk Render / development) ===

# Frontend URL untuk CORS (bisa multiple, pisahkan dengan koma)
# Contoh: https://chessweb.vercel.app,http://localhost:3000
# Untuk development lokal, tidak perlu diisi (default: http://localhost:3000)
CORS_ORIGIN=https://chessweb-nu.vercel.app/


GENERATE_SOURCEMAP=false
